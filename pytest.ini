[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --cov=engine
    --cov=ui
    --cov-report=html:htmlcov
    --cov-report=term-missing